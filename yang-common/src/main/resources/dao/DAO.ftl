package ${conf.basePackage}.${table.lowerCamelName}.dao;

import ${conf.basePackage}.base.dao.IBaseDAO;
import ${conf.basePackage}.${table.lowerCamelName}.entity.${table.className}Entity;

/**
 * ${table.comment} DAO
 * @author ${conf.author}
 * @version ${conf.version}
 * @date ${conf.createDate}
 */
public interface I${table.className}DAO extends IBaseDAO<${table.className}Entity>{
}
