package ${conf.basePackage}.${table.lowerCamelName}.manager;

import ${conf.basePackage}.base.manager.IBaseManager;
import ${conf.basePackage}.${table.lowerCamelName}.dto.${table.className}DTO;

/**
 * ${table.comment} Manager
 * @author ${conf.author}
 * @version ${conf.version}
 * @since ${conf.createDate}
 */
public interface I${table.className}Manager extends IBaseManager<${table.className}DTO>{

}
